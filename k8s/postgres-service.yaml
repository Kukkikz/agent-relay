apiVersion: v1
kind: Service
metadata:
  # Named "postgres" so it matches the hostname baked into agent-relay's
  # RELAY_DATABASE_URL below (Kubernetes gives every Service this cluster-DNS name).
  name: postgres
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
